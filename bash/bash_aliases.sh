alias info="info --vi-keys"
alias o=xdg-open
alias ll="ls -lh"
alias la="ls -la"
alias ls='ls --color=auto'
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias dic="sdcv"
alias githash="git rev-parse --short HEAD"
alias sshag='export SSH_AUTH_SOCK=`$repo_dir/sshag/sshag.sh`'
# the extra characters confuse scripts that don't expect it
# alias grep="grep --color=auto -n" # output line numbers
alias grep="grep --color=auto"

